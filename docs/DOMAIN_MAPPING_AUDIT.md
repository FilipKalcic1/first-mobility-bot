# Domain Mapping Audit (0.5A)

Generated by `scripts/phase_0_5_audit.py domain_mapping` on 2026-05-10 20:25:11.

## Summary

- Tools processed: 950
- Deterministic mapping (entity → domain): 950
- LLM fallback: 0
- Unmapped (OTHER): 0
- LLM cost: $0.0000 (0 calls)

## Per-domain tool counts

- **vehicle_info**: 1 tools
- **reservations**: 68 tools
- **mileage_reports**: 41 tools
- **cases**: 22 tools
- **people_org**: 153 tools
- **finance**: 147 tools
- **equipment_periodic**: 196 tools
- **fleet_management**: 74 tools
- **special_intents**: 0 tools
- **permissions_roles**: 28 tools
- **catalog_types**: 141 tools
- **tenants_lookup**: 79 tools

## Methodology

1. Build `entity_to_domain` index from `tool_domains.json` `primary_entities` arrays.
2. For each tool, extract entity from `operation_id` (drop method prefix, first underscore chunk).
3. Direct match → assign deterministically.
4. Prefix/substring match (e.g. `VehicleCalendarOn` ↔ `VehicleCalendar`) → assign with longest match wins.
5. No match → LLM call (gpt-4o-mini) with tool intent + 12 domain options.
6. LLM-returned `domain_id` validated against domain list; invalid → `OTHER`.

## Override table

For ambiguous entities present in 2+ domains:
- `CaseTypes` → `catalog_types` (more semantically specific than `cases`)
- `Master` → `people_org` (Persons batch-upsert, not vehicle MasterData)
- `MasterData` → `vehicle_info` (handled via longer prefix match naturally)

## Self-rate: see end of doc

## Sample LLM-classified tools (first 10 of 0)


## OTHER bucket (0 tools — needs Filip review)


## Self-rating: 9/10

- ✓ All 950 tools assigned a domain
- ✓ Deterministic mapping covers ~100% (predictable, traceable)
- ✓ LLM fallback used only for ambiguous 0 tools
- ✓ Output files validated (counts add up)
- Cap at 9 not 10 because 0 tools landed in OTHER (needs Filip review for manual fix or domain expansion)
